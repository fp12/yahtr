<GameConsole>:
    messages: messages
    
    canvas:
        Color:
            rgba: 0,0,0,0.5
        Rectangle:
            pos: root.pos
            size: root.size
    Label:
        id: messages
        pos: root.pos
        markup: True
        size_hint_y: None
        height: self.texture_size[1]
        text_size: self.width, None
        font_size: '12sp'
